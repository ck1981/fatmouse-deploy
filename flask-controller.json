{
    "id": "flask-controller-${FAM_DEPLOY_VERSION}",
    "kind": "ReplicationController",
    "apiVersion": "v1beta1",
    "desiredState": {
        "replicas": 3,
        "replicaSelector": {"name": "flask", "version": "${FAM_DEPLOY_VERSION}"},
        "podTemplate": {
            "desiredState": {
                "manifest": {
                    "version": "v1beta1",
                    "id": "flask",
                    "containers": [{
                        "name": "flask",
                        "image": "gcr.io/apt_entropy_87912/flask-test:${FAM_DEPLOY_RELEASE_VERSION}",
                        "ports": [{"name": "flask-server",  "containerPort": 5000}],
                        "env": [{"name": "DEPLOY_NAME", "value": "${FAM_DEPLOY_NAME}"}],
                    }]
                }
            },
            "labels": {"name": "flask", "version": "${FAM_DEPLOY_VERSION}"}
        }
    },
    "lables": {"name": "flask", "version": "${FAM_DEPLOY_VERSION}"}
}
